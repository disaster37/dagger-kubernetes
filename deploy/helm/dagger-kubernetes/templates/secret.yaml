{{- if .Values.supervisor.enabled }}
{{- if and .Values.auth.bootstrapAdmin.password (not .Values.auth.bootstrapAdmin.secretRef.name) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dagger-kubernetes.fullname" . }}-admin-password
  namespace: {{ include "dagger-kubernetes.namespace" . }}
  labels:
    {{- include "dagger-kubernetes.labels" . | nindent 4 }}
type: Opaque
stringData:
  password: {{ .Values.auth.bootstrapAdmin.password | quote }}
{{- end }}
{{- if and .Values.auth.jwt.secret (not .Values.auth.jwt.secretRef.name) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dagger-kubernetes.fullname" . }}-jwt
  namespace: {{ include "dagger-kubernetes.namespace" . }}
  labels:
    {{- include "dagger-kubernetes.labels" . | nindent 4 }}
type: Opaque
stringData:
  secret: {{ .Values.auth.jwt.secret | quote }}
{{- end }}
{{- if and .Values.auth.oauth.clientSecret (not .Values.auth.oauth.clientSecretRef.name) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dagger-kubernetes.fullname" . }}-oauth
  namespace: {{ include "dagger-kubernetes.namespace" . }}
  labels:
    {{- include "dagger-kubernetes.labels" . | nindent 4 }}
type: Opaque
stringData:
  client_secret: {{ .Values.auth.oauth.clientSecret | quote }}
{{- end }}
---
apiVersion: v1
kind: Secret
metadata:
  name: engine-registry-auth
  namespace: {{ include "dagger-kubernetes.namespace" . }}
  labels:
    {{- include "dagger-kubernetes.labels" . | nindent 4 }}
type: Opaque
stringData:
  token: {{ .Values.supervisor.config.cache.authToken | default "placeholder" | quote }}
# .dockerconfigjson is ALREADY base64-encoded (the operator supplies the same
# value a `kubectl create secret docker-registry` would store). It goes into
# `data:` (not `stringData:`) so Kubernetes does NOT base64-encode it a second
# time; the kubelet then decodes it exactly once, so engine pods read raw JSON
# at /etc/dagger/.dockerconfigjson. `e30K` is base64 of `{}`.
data:
  .dockerconfigjson: {{ .Values.supervisor.config.fleet.engineDockerConfig | default "e30K" | quote }}
{{- if and .Values.supervisor.dataplane.tls.caCrt .Values.supervisor.dataplane.tls.caKey }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dagger-kubernetes.fullname" . }}-minting-ca
  namespace: {{ include "dagger-kubernetes.namespace" . }}
  labels:
    {{- include "dagger-kubernetes.labels" . | nindent 4 }}
type: Opaque
stringData:
  ca.crt: {{ .Values.supervisor.dataplane.tls.caCrt | quote }}
  ca.key: {{ .Values.supervisor.dataplane.tls.caKey | quote }}
{{- end }}
# When supervisor.dataplane.tls.caCrt/caKey are empty (the default), the
# minting CA is NOT rendered here: the supervisor auto-bootstraps it on first
# boot (ordinal 0 generates a goca CA and writes it to <release>-minting-ca;
# other pods poll it). No manual certificate generation is required. Only set
# them to bring an existing CA (e.g. migrating from a prior deployment).
{{- if and .Values.supervisor.dataplane.tls.crt .Values.supervisor.dataplane.tls.key }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dagger-kubernetes.fullname" . }}-tls
  namespace: {{ include "dagger-kubernetes.namespace" . }}
  labels:
    {{- include "dagger-kubernetes.labels" . | nindent 4 }}
type: kubernetes.io/tls
data:
  tls.crt: {{ .Values.supervisor.dataplane.tls.crt | b64enc | quote }}
  tls.key: {{ .Values.supervisor.dataplane.tls.key | b64enc | quote }}
{{- end }}
{{- end }}

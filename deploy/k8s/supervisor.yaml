apiVersion: apps/v1
kind: Deployment
metadata:
  name: supervisor
  namespace: dagger-kubernetes
  labels:
    app: supervisor
spec:
  replicas: 2
  selector:
    matchLabels:
      app: supervisor
  template:
    metadata:
      labels:
        app: supervisor
    spec:
      serviceAccountName: supervisor
      containers:
        - name: supervisor
          image: dagger-kubernetes/supervisor:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: control
              containerPort: 8080
            - name: data
              containerPort: 8443
            - name: raft
              containerPort: 8081
          args:
            - "--config=/etc/dagger-kubernetes/config.app.yaml"
          env:
            - name: DAGGER_KUBERNETES_LOG_LEVEL
              value: "info"
            - name: DAGGER_KUBERNETES_DATABASE_DIR
              value: "/var/lib/dagger-kubernetes"
            - name: DAGGER_KUBERNETES_RAFT_BIND_ADDR
              value: ":8081"
          volumeMounts:
            - name: config
              mountPath: /etc/dagger-kubernetes/config.app.yaml
              subPath: config.app.yaml
              readOnly: true
            - name: tokens
              mountPath: /etc/dagger-kubernetes/tokens
              subPath: tokens
              readOnly: true
            - name: tls
              mountPath: /etc/dagger-kubernetes/tls
              readOnly: true
            - name: db
              mountPath: /var/lib/dagger-kubernetes
          readinessProbe:
            httpGet:
              path: /readyz
              port: control
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: control
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: config
          configMap:
            name: supervisor-config
        - name: tokens
          secret:
            secretName: supervisor-tokens
            optional: true
        - name: tls
          secret:
            secretName: supervisor-tls
        - name: db
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: supervisor-control
  namespace: dagger-kubernetes
spec:
  selector:
    app: supervisor
  ports:
    - name: http
      port: 80
      targetPort: control
---
apiVersion: v1
kind: Service
metadata:
  name: supervisor-data
  namespace: dagger-kubernetes
spec:
  selector:
    app: supervisor
  type: LoadBalancer
  ports:
    - name: tls
      port: 443
      targetPort: data
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: supervisor-ingress
  namespace: dagger-kubernetes
spec:
  rules:
    - host: supv.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: supervisor-control
                port:
                  number: 80
    # Cache vhost: engines push/pull cache blobs through the Supervisor proxy
    # on this hostname. The control-plane TLS certificate MUST include
    # `cache.supv.example.com` as a SAN (the cache vhost shares the control-plane
    # TLS listener).
    - host: cache.supv.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: supervisor-control
                port:
                  number: 80
